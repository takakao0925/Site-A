export default function App() {
  return (
    <div
      style={{
        fontFamily: 'system-ui, sans-serif',
        padding: '2rem',
        color: '#f4f6fa',
        background: '#05070c',
        minHeight: '100vh',
      }}
    >
      <h1>React + Vite environment ready</h1>
      <p>
        This is a separate playground from the main static site
        (index.html / main.js / styles.css at the project root), which is
        untouched. Build here, then decide later what (if anything) moves
        into the live site.
      </p>
    </div>
  );
}
